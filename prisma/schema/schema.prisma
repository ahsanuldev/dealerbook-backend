generator client {
  provider        = "prisma-client"
  output          = "../../src/generated/prisma/client"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
}

